# sea.AI Badcase 排查分析报告模板

## 1. 基本信息

Trace ID：`<trace-id>`　｜　会话 ID：`<session-id>`　｜　总耗时：`<duration>`　｜　[查看调试记录](<trace-url>)

| 项目 | 内容 |
|---|---|
| 用户提问原句 | `<user-prompt>` |
| 实际最终回复 | `<final-output>` |
| 专家节点已生成的回复 | `<expert-output；没有则写“无”>` |
| 预期结果 | `<expected-result>` |

## 2. 排查结论

`<正常 / 异常 / 证据不足>`。

`<用两至三段说明异常发生在哪里、哪个配置覆盖或误导了结果，以及主要异常位置。>`

主要异常位置：

> `<配置层级完整路径>`

配置链路：

> `<应用层路径>`  
> `<接待对象及版本>`  
> `<子 Agent / 专家层路径>`  
> `<最终输出控制路径>`

## 3. 关键链路与配置定位

| 执行环节 | 本次执行结果 | sea.AI 配置位置 | Trace 证据 | 判断 |
|---|---|---|---|---|
| `<环节>` | `<结果>` | `<当前配置路径>` | `<节点>` | `<判断>` |

## 4. 根因与优化策略

### P0：`<直接导致 Badcase 的问题>`

`<说明为何必须最先修复。>`

#### 1. `<优化动作>`

根因：

`<说明当前规则如何导致本次结果，并引用必要的 Trace 事实。>`

| 项目 | 原提示词/规则 | 建议更新后提示词/规则 |
|---|---|---|
| 配置位置 | `<完整配置路径>` |  |
| 优化对比 | `<当前页面原文>` | `<可直接替换的新规则>` |

优化目的：`<一句话>`

### P1：`<影响识别、路由或专家执行的问题；没有则写“本次无对应优化动作”>`

`<按相同格式继续编号；没有则不添加编号项。>`

### P2：`<质检、防御或低优先级问题；没有则写“本次无对应优化动作”>`

`<按相同格式继续编号；没有则不添加编号项。>`

## 5. 建议复测用例与验收标准

### 复测用例

| 测试问题 | 预期结果 |
|---|---|
| `<原 Badcase>` | `<修复后的结果>` |
| `<边界用例>` | `<不应误命中>` |
| `<明确异常用例>` | `<应命中对应处理>` |

### 验收标准

1. `<原 Badcase 的正确结果>`
2. `<关键节点与最终输出一致>`
3. `<边界场景不误判>`
