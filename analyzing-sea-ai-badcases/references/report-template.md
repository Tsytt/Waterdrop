# sea.AI Badcase 排查分析报告模板

## 1. 基本信息

Trace ID：`<trace-id>`　｜　会话 ID：`<session-id>`　｜　总耗时：`<duration>`　｜　[查看调试记录](<trace-url>)

| 项目 | 内容 |
|---|---|
| 用户提问原句 | `<user-prompt>` |
| 实际最终回复 | `<final-output>` |
| 专家节点已生成的回复 | `<expert-output；没有则写“无”>` |
| 预期结果 | `<expected-result>` |

## 2. 排查结论

`<正常 / 异常 / 证据不足>`。

### 业务期望与路由边界判断

| 判断项 | 结论 |
|---|---|
| 期望处理方式 | `<直接回答 / 转人工 / 先回答后转人工；说明业务依据>` |
| 期望接待链路 | `<应用层意图 / Agent → FAQ / 知识库 / 专家 → 最终处理>` |
| 实际接待链路 | `<Trace 实际命中的意图 / Agent → 实际执行节点 → 最终处理>` |
| 当前意图 / Agent 是否应接待 | `<是 / 否 / 证据不足；说明正确接待方>` |
| 路由边界结论 | `<应用层路由合理，继续下钻 / 当前意图边界过宽或接待顺序导致截流 / 证据不足>` |

`<用两至三段说明首次异常发生在哪里。若当前意图 / Agent 不应接待，优先说明应用层 Agent 接待规则的边界或顺序问题；不要因为下游固定转人工、专家未执行或汇总覆盖而把主根因下移。若当前路由合理，再说明哪个下游配置覆盖或误导了结果。>`

主要异常位置：

> `<配置层级完整路径>`

配置链路：

> `<应用层路径>`  
> `<接待对象及版本>`  
> `<子 Agent / 专家层路径>`  
> `<最终输出控制路径>`

## 3. 关键链路与配置定位

| 执行环节 | 本次执行结果 | sea.AI 配置位置 | Trace 证据 | 判断 |
|---|---|---|---|---|
| `<环节>` | `<结果>` | `<当前配置路径>` | `<节点>` | `<判断>` |

第一行优先记录应用层意图识别及接待规则，至少对照当前接待方与候选正确接待方；随后再记录子 Agent、专家、FAQ / 知识库、转人工、汇总和质检等实际关键环节。未执行的期望链路可将 Trace 证据写为“未进入该链路”，但仍应填写已核实的配置位置。

## 4. 根因与优化策略

### P0：`<首次导致 Badcase 的路由边界或直接原因>`

`<说明为何必须最先修复。若问题被错误意图 / Agent 截流，P0 优先写应用层 Agent 接待规则：收窄特殊场景边界，并补强正确答疑意图 / Agent 的承接；不要先优化下游汇总节点。>`

#### 1. `<优化动作>`

根因：

`<说明当前规则如何导致本次结果，并引用必要的业务期望、路由对照和 Trace 事实。>`

| 项目 | 原提示词/规则 | 建议更新后提示词/规则 |
|---|---|---|
| 配置位置 | `<完整配置路径>` |  |
| 优化对比 | `<当前页面原文>` | `<可直接替换的新规则>` |

优化目的：`<一句话>`

### P1：`<正确接待方的承接补强或子 Agent / 专家执行问题；没有则写“本次无对应优化动作”>`

`<按相同格式继续编号；没有则不添加编号项。>`

### P2：`<下游转人工、汇总、质检、防御或低优先级兜底问题；没有则写“本次无对应优化动作”>`

`<按相同格式继续编号；没有则不添加编号项。>`

## 5. 建议复测用例与验收标准

### 复测用例

| 测试问题 | 预期结果 |
|---|---|
| `<原 Badcase>` | `<命中期望意图 / Agent，并得到修复后的结果>` |
| `<与原问题同类的可答边界用例>` | `<由答疑意图 / FAQ / 知识库承接，不被特殊场景截流>` |
| `<明确需要转人工的特殊场景用例>` | `<仍命中特殊场景并按规则转人工>` |

### 验收标准

1. `<原 Badcase 的正确结果>`
2. `<实际接待链路与期望接待链路一致>`
3. `<可答问题不被特殊场景误判，真实特殊场景不漏接>`
